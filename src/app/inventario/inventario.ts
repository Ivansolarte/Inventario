export class Inventario {
    // todos locas campos de la clase (los campos de la tabla usuario)
    constructor(
        public id: number,
        public nombre: string,
        public apellido: string,

    ) {}
}
